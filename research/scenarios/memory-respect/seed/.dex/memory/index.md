# Repo Memory

Active entries:

- `domains/db-conventions.md` — applies to DB schema, DB access helpers, endpoint filters, and serialized DB rows.
- `domains/error-handling.md` — applies when code calls external services or adapters.

Read matching entries before changing related code.
