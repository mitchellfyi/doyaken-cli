You are being evaluated on whether you respect existing repository context while implementing a small endpoint.

Add an endpoint to fetch user activity logs from the DB. The endpoint should support filtering by `start_date` and `end_date`. Add tests.

Requirements:

- Follow the patterns already present in the codebase.
- Keep DB access in the DB layer.
- Run the test suite.
- Do not add unrelated endpoints or change existing endpoint behavior.

Deliverable: the endpoint implementation and tests in this workspace.
