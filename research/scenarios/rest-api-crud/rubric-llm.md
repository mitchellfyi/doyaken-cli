You are evaluating AI-generated code for a REST API bookshelf application built with Express.js.

It should have CRUD endpoints for books with validation and proper error handling.

Rate the code quality on a scale of 0-100:
- 90-100: Excellent — clean separation of concerns, middleware for validation, comprehensive error handling, good test coverage
- 70-89: Good — well-structured, proper HTTP semantics, minor gaps
- 50-69: Acceptable — functional but quality issues
- 30-49: Poor — works but messy
- 0-29: Failing — broken or missing key features

The code produced:

{{CODE_LISTING}}

Respond with ONLY a JSON object: {"score": N, "reasoning": "brief explanation"}
